# Ralph Progress Log
Started: Fri Jul 24 2026
---
US-001: added percentForTime/timeForPercent/timeAxisGridlines to lib/schedule/hours.ts (horizontal equivalents of offsetForTime/timeForOffset, which stay untouched); added lib/schedule/hours.test.ts and a vitest.config.ts path alias so hours.ts's existing "@/" imports resolve under vitest; typecheck, lint, and npm test (53 tests) all pass.
US-002: renamed lib/schedule/now-line.ts's useNowOffsetPx to useNowOffsetPercent (percentForTime-based); components/schedule/now-line.tsx now renders a vertical border-l-2 line at left:X% with the dot at top-left, spanning full height, instead of the old horizontal border-t-2/top-offset line. typecheck and lint pass; verified against the real dev server (linked remote Supabase) with a temp Playwright test user/seeded booking — rendered left:% matches percentForTime math exactly and disappears on non-today dates.
US-003: EventBlock now positions via left/width percentages from percentForTime(startTime/endTime); top/height reinterpreted as caller-supplied lane position (px, for US-005's stacking). Rendered width measured via ResizeObserver (border-box) drives 3 text-density tiers: >=90px full detail, 40-89px title-only truncated, <40px no text (existing hover Popover unaffected). DragCreateOverlay switched from top/height to left/width percent props. Updated desktop-resource-grid.tsx and mobile-day-calendar.tsx call sites to the new prop contracts (their old vertical-time visual correctness isn't preserved mid-refactor, consistent with US-002's precedent — both are replaced by TimelineGrid in US-004/005/012). typecheck, lint, npm test (53 tests) all pass; verified in the real dev server (linked remote Supabase, temp test user + seeded booking, cleaned up after) — forcing rendered widths through 200/70/60/45/20px confirmed the three tiers at exactly the 90/40px boundaries, collapsed-block hover still showed the full popover, and a drag produced a correctly time-labeled ghost overlay with no console errors.
US-004: added components/schedule/timeline-grid.tsx (sticky room column + sticky time header shell, flexbox layout, max-h-[70vh] internal scroll pane so sticky actually has something to stick within); added category_color to ScheduleRoom + schedule page query. typecheck, lint, npm test (53 tests) pass; verified in real dev server with a temp 30-room fixture page (deleted after) and a temp admin-API test user (deleted after) across 390/800/1400/1900px widths - caught and fixed two real bugs found via browser verification: sticky header not sticking (no bounded scroll container) and hour-label text collision/clipping at narrow widths (axis now has a 560px min-width, forcing native horizontal scroll on mobile).
