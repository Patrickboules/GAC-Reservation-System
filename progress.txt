# Ralph Progress Log
Started: Tue Jul 21 14:28:31 EEST 2026
---
US-001: added canvas/surface/line/ink/sky/sand/status color tokens and radius(sm/md/lg/xl)/shadow(sm/md/lg) tokens to app/globals.css's @theme block per UI-Redesign-Spec.md 1.1. npx tsc --noEmit, npm run lint, npm run build all pass; compiled CSS confirmed to contain the new utility classes.
US-002: loaded Bricolage Grotesque/Geist/Geist Mono via next/font/google in app/layout.tsx; wired font-sans/font-mono/font-display tokens in app/globals.css and added tabular-nums for font-mono. typecheck, lint, build all pass; confirmed compiled CSS resolves each utility to the correct font var. Also fixed local-only .env (added NEXT_PUBLIC_SUPABASE_ANON_KEY alias) so dev server could boot for verification — no dev-browser skill present in this env.
US-003: applied canvas/ink-700 body defaults and ink-900/font-display heading defaults in app/globals.css @layer base; switched Card (components/ui/card.tsx) to bg-surface/ring-line; added type-scale (display-xl/display/h2/h3/body/small/caption) as reusable Tailwind text-* utilities. typecheck, lint, build pass; verified compiled CSS and a temporary probe route (removed after) show correct output; dev server /login response confirms canvas background, ink-700 body text, ink-900 headings, and surface-token Card rendering.
US-004: added components/kit/button.tsx (Button: primary/secondary/ghost/danger variants, sm/md/lg sizes, width-locked loading spinner, focus-visible ring-sky-300) and components/kit/icon-button.tsx (IconButton: same variants/sizes at square dimensions, required aria-label, optional tooltip via @base-ui/react/tooltip). typecheck, lint, build pass; verified via a temporary /kit-probe route (reverted) rendering all states plus compiled-CSS spot checks for bg-sky-600, status-rejected fg/bg, rounded-md, ring-sky-300, animate-spin.
US-005: added components/kit/status-badge.tsx, filter-chip.tsx, segmented-control.tsx per UI-Redesign-Spec.md 3.1/3.2. typecheck and lint pass; verified via a temporary /kit-probe route (added to middleware PUBLIC_PATHS, then both reverted) — curl'd server-rendered HTML and confirmed all status bg/fg token classes, dot markup, data-selected/data-pressed states, and count/remove-button markup render correctly (no headless browser available in this env, same limitation noted in US-002/US-004).
US-006: added components/kit/input.tsx, select.tsx, textarea.tsx with default/focus/error/disabled states, label/helperText/errorText slots, aria-describedby wiring, and a non-color error indicator (AlertCircle icon). Select wraps @base-ui/react/select. Verified: typecheck/lint/build clean; Playwright-driven /kit-probe route (temporary, reverted) confirmed focus ring, error styling, disabled state, and select option selection resolving the correct label.
US-007: added components/kit/date-picker.tsx (inline/popover modes, sky-600 selected fill, sky-300 today ring, muted disabled dates) and components/kit/time-range-picker.tsx (font-mono steppers snapping to BOOKING_TIME_STEP_MINUTES, live duration readout, hasConflict inline warning); added time helpers to lib/dates.ts and lib/bookings/time-granularity.ts. typecheck/lint/build pass; verified via temporary /kit-probe route with Playwright (screenshot + DOM assertions), then reverted the probe route and middleware change.
US-008: added components/kit/modal.tsx (Modal/ModalTrigger/ModalContent/ModalHeader/ModalFooter/ModalTitle/ModalDescription/ModalClose) on @base-ui/react/dialog — one Dialog.Popup styled as a bottom sheet with drag handle below md, a centered max-w-lg modal at md+, backdrop rgba(16,32,64,.32), closes on backdrop click/Escape, 220ms slide+fade or scale+fade that collapses to fade-only under motion-reduce:. typecheck/lint/build pass; verified via temporary /kit-probe route with Playwright at desktop (1280x800) and mobile (375x700) viewports plus a reducedMotion:'reduce' context, then reverted the probe route and middleware change.
US-009: added components/kit/toast.tsx on @base-ui/react/toast (ToastProvider mounting Portal+Viewport top-right desktop/top mobile, useToast() hook with success/info/warning/error methods, action-link support, 4s auto-dismiss, reduced-motion-safe transition, left accent + icon per variant); mounted ToastProvider in app/layout.tsx so any client component can call useToast(). typecheck/lint/build pass; verified via temporary /kit-probe route with Playwright (all 4 variants, auto-dismiss, close button, action link, mobile top positioning, reduced-motion transform), then reverted the probe route and middleware change.
US-010: added components/kit/empty-state.tsx, error-state.tsx, loading-state.tsx (list/cards/rows skeleton variants), skeleton.tsx, tooltip.tsx, and dropdown-menu.tsx (@base-ui/react tooltip/menu primitives). typecheck/lint/build pass; verified via temporary /kit-probe route with Playwright (screenshots + DOM assertions), then reverted the probe route and middleware change.
US-011: added components/kit/table.tsx — generic Table<T> with sticky header, sortable columns (aria-sort + toggle), row hover bg-sky-50, status column via US-005 StatusBadge, trailing kebab row-actions via US-010 DropdownMenu, skeleton loading rows + EmptyState via US-010 primitives, pagination footer. typecheck/lint/build pass; verified via temporary /kit-probe route with Playwright (sort toggle, hover bg, kebab menu, pagination, loading skeletons, empty state), then reverted the probe route and middleware change.
US-012: added components/kit/avatar.tsx (initials fallback, sm/md/lg) and role-badge.tsx (member/servant/scout/guide/admin, new role-*-fg/bg tokens in globals.css); verified via temporary /kit-probe route with Playwright (reverted), typecheck and lint pass.
US-013: added components/kit/room-card.tsx (RoomCard) — whole-card Link tap target, color header, name+font-mono code, capacity, up to 4 amenity icons + overflow, location line, live free/busy availability dot; grid and list density variants. typecheck/lint/build pass; verified via temporary /kit-probe route with Playwright (grid+list, overflow, availability labels, missing-data fallback, tap target, no 375px h-scroll, focus ring confirmed correct after ruling out a transition-timing artifact in the probe itself), then reverted the probe route and middleware change.
US-014: added components/kit/booking-card.tsx (BookingCard) — left color stripe by status, room/date/font-mono time range, service chip, US-005 StatusBadge, trailing kebab menu (US-010 DropdownMenu) with Modify/Cancel where handlers supplied, Cancel guarded by a confirm Modal (US-008), pending shows "Awaiting approval". typecheck/lint/build pass; verified via temporary /kit-probe route with Playwright (stripe colors, awaiting-approval line, menu open/Modify/Cancel, confirm modal open/dismiss, no-menu when no handlers, no 375px h-scroll), then reverted the probe route and middleware change.
US-015: added supabase/migrations/20260721000000_create_notifications_schema.sql — notifications table (user_id FK, type check constraint 'reminder'|'approved'|'rejected'|'cancelled'|'admin_new_request', nullable booking_id FK, message, read_at, created_at), RLS enabled with select/update-own policies only (no insert policy — inserts are service-role only, per US-016), authenticated/service_role grants matching existing table conventions. Verified with npx supabase db reset (migration applies cleanly) + psql inspection of pg_policies/information_schema grants confirming no client insert path; typecheck passes.
US-016: added lib/notifications.ts (pure message formatters + notifyBookingApproved/Rejected/Cancelled/notifyAdminsNewRequest, each using createAdminClient from US-015's admin.ts to insert via service-role, matching the notifications table's no-client-insert RLS). Wired into existing app/admin/actions.ts approveBooking/rejectBooking (rejectBooking now fetches the booking first, mirroring approveBooking, to have the data needed to notify) and app/bookings/actions.ts requestBooking (notifies all admin profiles) and cancelBooking (notifies the requester on the existing owning-member cancel path — no separate admin-cancel action exists yet in this codebase). No duplicate business logic introduced. Verified: npx tsc --noEmit, npm run lint, npm run build all clean; added lib/notifications.test.ts (10 tests, message formatting + insert payloads via a fake Supabase client) and npm test passes 20/20.
US-017: added components/bookings/booking-sheet.tsx (BookingSheet) replacing the old request-form.tsx, built on the US-008 Modal/Sheet with US-007 date/time-range pickers, a live conflict warning wired to lib/bookings/conflict-check, the unchanged BOOKING_SERVICES dropdown, notes, an optimistic US-014 BookingCard + US-009 toast on submit, and the unchanged requestBooking server action. Deleted components/bookings/request-form.tsx (no remaining imports). typecheck/lint/build clean; no dev-browser skill available, so verified against the real dev server (which points at the linked remote Supabase project) via Playwright with temporary, fully-cleaned-up test users/bookings — confirmed mobile bottom sheet, desktop centered modal, prefill via query params, live conflict warning, optimistic pending card + toast, and redirect to /bookings. Along the way fixed a real local .env gap (missing SUPABASE_SERVICE_ROLE_KEY, causing requestBooking to 500 after insert instead of redirecting) but deliberately left alone a separate, higher-risk gap: the US-015 notifications migration was never pushed to the remote database (local-only per `supabase migration list --linked`), and no profile has role='admin' on remote — flagged to the user instead of pushing schema changes unprompted.
US-018: added components/bookings/my-bookings-tabs.tsx (US-005 SegmentedControl w/ per-tab counts + US-014 BookingCard rows + per-bucket US-010 EmptyState) and rewired app/bookings/page.tsx to feed it from the existing unchanged bucketForBooking grouping, removing the old shadcn Tabs/Card markup. typecheck/lint/build pass; verified against the real dev server with Playwright + a temporary test user/4 seeded bookings (one per bucket, cleaned up) — correct counts, tab switching, "Awaiting approval" line, zero horizontal scroll at 375px/1280px. Noted a pre-existing, unrelated Base UI "nativeButton" console warning that already fires app-wide (incl. the unmodified Home page) whenever Button uses render={<Link>} — not introduced by this story, left as-is.
US-019: extended components/bookings/booking-sheet.tsx with an optional `booking` prop so the same US-017 sheet handles editing — pre-fills existing values, submits via the unchanged updateBooking action, excludes the booking's own row from the live conflict check, and shows a dynamic "revert to pending, needs re-approval" note when room/date/time changes on an approved booking. Rewrote app/bookings/[id]/edit/page.tsx to use it and deleted the now-superseded components/bookings/edit-booking-form.tsx. typecheck/lint/build clean; verified against the real dev server with Playwright using a temporary test user + seeded pending/approved bookings (cleaned up after) — pending edit saves without a status change, approved edit shows the note only after a real change and correctly reverts status to pending on save (confirmed via direct DB query).
US-020: replaced components/bookings/cancel-booking-button.tsx's legacy shadcn AlertDialog with the US-008 kit Modal (same cancelBooking server action, unchanged); typecheck/lint/build pass; verified in real dev server with Playwright (temp user + approved booking) — modal renders with no legacy alert-dialog elements, confirming cancels the booking (DB status -> cancelled) and redirects to /bookings.
US-021: added components/shell/sidebar.tsx (server component, checks profiles.role server-side) + sidebar-nav.tsx (client component, collapse toggle + active-route highlighting via usePathname). Primary nav Schedule/Book(/bookings/new)/My Bookings/Rooms; Admin group (Dashboard/Requests/Manage Rooms/Reports) rendered only when isAdmin (computed server-side, passed as a prop -- never client-decided). Active item: sky-100 bg, sky-600 text, 3px left indicator; collapses between 240px and 72px. Not wired into app/layout.tsx yet (that's US-024). typecheck/lint clean; verified against the linked remote Supabase dev server with Playwright (Sidebar temporarily mounted in layout, then reverted) using a temporary admin test user created/deleted via the service-role admin API -- confirmed admin group visibility, active-state styling, and collapse/expand.
US-022: added components/shell/mobile-shell.tsx, mobile-top-bar.tsx (placeholder), and mobile-tab-bar.tsx -- fixed bottom nav (Schedule/Book/My Bookings, sky-600 active/ink-500 inactive) plus a More button opening a US-008 Modal bottom sheet with Rooms and a server-checked Admin group. Not wired into layout.tsx (US-024). typecheck/lint clean; verified with Playwright at 375x700 against the real dev server using a temp admin test user -- confirmed tab bar items, active styling, sheet contents, and real navigation from sheet links; no h-scroll on /bookings at 375px.
US-023: added components/shell/top-bar.tsx (shared client TopBar with a left date-context slot, and right-side search/notifications/profile controls) plus components/shell/desktop-top-bar.tsx (new server component, own-profile fetch pattern like sidebar.tsx); extended mobile-shell.tsx/mobile-top-bar.tsx to fetch+pass the same profile for mobile. Search opens a placeholder Modal; notifications bell shows an unread dot + stub DropdownMenu ("You're all caught up", real data in US-025); profile menu shows Avatar+name+RoleBadge (US-012) with a working Sign out. Not wired into app/layout.tsx yet (US-024). typecheck/lint/build clean; verified against the real dev server with Playwright (temporarily mounted Sidebar/DesktopTopBar/MobileShell in layout.tsx, reverted after) using a temp admin test user -- confirmed search panel, notifications panel, profile menu (name+Admin badge) at 1280px and 375px, working sign-out, and zero h-scroll on mobile. Fixed two real bugs this story's code introduced: a tooltipped IconButton used as a DropdownMenuTrigger broke Base UI's native-button semantics (dropped the tooltip there), and DropdownMenuGroupLabel used outside DropdownMenuGroup crashed the notifications panel (wrapped it properly). Left alone the pre-existing, already-flagged (US-018+) nativeButton/hydration console warnings from the legacy components/ui/button.tsx render={<Link>} pattern.
US-024: moved every authenticated route into app/(app)/ (admin, availability, bookings incl. [id]/edit and new, rooms incl. [id], schedule, home) and added app/(app)/layout.tsx rendering components/shell/app-shell.tsx, which composes Sidebar+DesktopTopBar (desktop, US-021/023) and MobileShell (mobile, US-022/023) around every route; /login and /signup stay shell-free outside the group. Updated moved-actions import paths in booking-sheet.tsx/cancel-booking-button.tsx and switched page wrappers from min-h-screen to min-h-full. Fixed a real bug the acceptance criteria caught: /schedule's legacy 4-button nav row overflowed at 375px, now redundant given the shell's nav -- added flex-wrap rather than redesigning the page (full redesign is US-027/028). Verified: tsc/lint/build clean (13 routes, URLs unaffected by the route group); Playwright against the real dev server with temp member+admin test users and a seeded room/booking (all cleaned up) confirmed zero horizontal scroll at 375px on all 13 authenticated routes, shell renders on every route, /login is shell-free, and the only console warnings present are the already-flagged pre-existing nativeButton ones (not introduced by this story).
US-025: wired real notifications data into the top bar — lib/notifications.ts's new getRecentNotifications() feeds components/shell/desktop-top-bar.tsx/mobile-shell.tsx, which pass notifications+unreadCount into components/shell/top-bar.tsx; the notifications DropdownMenu renders the real list (type icon/color, relative time, unread bold+dot, links to /bookings/[id]) and opening it marks the shown notifications read via a new app/(app)/notifications/actions.ts server action (RLS-scoped update + revalidatePath). typecheck/lint/build/tests all pass. Verified against the local Supabase stack (db reset applies the still-unpushed-to-remote US-015 migration cleanly) with Playwright at 1280px/375px: unread dot before open, correct panel rendering for all 4 notification types, read_at persisted server-side (confirmed via psql + a fresh browser context), and notification-click navigation to the booking. Restored .env to the remote project and stopped the local stack after. Remote still needs `supabase db push` (blocked here by lack of an interactive `supabase login`) before this feature is live in production.
US-026: added lib/notifications.ts's ensureReminderNotifications(admin, userId) — finds the user's approved bookings starting within 24h, skips ones that already have a reminder row, inserts formatReminderMessage() rows for the rest; called from components/shell/desktop-top-bar.tsx and mobile-shell.tsx before every getRecentNotifications() fetch (on-demand, no scheduler). Added supabase/migrations/20260722000000_add_reminder_notification_unique_index.sql (partial unique index on notifications(booking_id) where type='reminder') as a DB-level backstop, swallowing 23505 races. The reminder clock icon in the panel (components/shell/top-bar.tsx NOTIFICATION_ICONS) was already in place from US-025. typecheck/lint/build clean; npm test passes 26/26 (5 new tests: within-window insert, dedupe against existing reminder, >24h skip, already-started skip, no-bookings no-op); `npx supabase db reset` applies the new migration cleanly locally.
US-027: added components/schedule/mobile-day-calendar.tsx (room Select + font-mono hour axis 8AM-11PM from new lib/schedule/hours.ts constants + full-width event blocks positioned by start/end time, sand-100 off-hours bands, touch-swipe/IconButton day nav) shown lg:hidden on /schedule alongside the existing desktop ScheduleView (now wrapped `hidden lg:block`). Verified: tsc/lint/build clean; Playwright against the real dev server with a temp test user + seeded today-only booking at a 375px hasTouch viewport confirmed room selector default/visibility, 8AM/11PM labels, zero horizontal scroll, 2 sand-100 bands, the booking block, a dispatched swipe changing the date label and (scoped to the mobile-only container, since the hidden desktop ScheduleView independently shows a 7-day-from-today window and produced a page-wide false positive in the first pass) hiding/showing the booking correctly, and Previous-day restoring it.
US-028: added components/schedule/desktop-resource-grid.tsx — rooms as CSS-grid columns (min 132px) in one overflow-auto container with sticky header row + sticky time gutter (reusing US-027's now-shared lib/schedule/hours.ts helpers), sand-100 off-hours bands, and a scroll-tracked edge-fade hint. Replaced the legacy date-range ScheduleView on /schedule's desktop slot and deleted the now-unused schedule-view.tsx. tsc/lint/build clean; verified with Playwright (temp user + seeded booking) — 143.5px columns, sticky gutter/header confirmed via before/after-scroll bounding boxes, booking block renders, zero h-scroll and mobile calendar unaffected at 375px.
US-029: added components/schedule/day-strip.tsx (scrollable role=tablist day-pill strip: left/right arrow buttons, Today button disabled-when-current, selected pill gets sky-600 fill, auto-scrolls selection into view) and components/schedule/schedule-content.tsx (client wrapper owning the shared `date` state, replacing direct rendering of MobileDayCalendar/DesktopResourceGrid on app/(app)/schedule/page.tsx so the strip and both calendar views stay in sync). Both calendar views now take `date` as a prop instead of managing it internally; removed MobileDayCalendar's now-redundant prev/next+label row (superseded by the strip) while keeping its swipe gesture. Added lib/schedule/day-transition.ts's useDayTransitionDirection hook + dayTransitionClassName, used to key each calendar's grid content by date and apply a 300ms slide-right/slide-left animation (new keyframes in app/globals.css) matching navigation direction; a prefers-reduced-motion media query re-declares the same keyframes names with the transform stripped, leaving only the fade. Verified: tsc/lint/build clean (13 routes unchanged). Local Supabase/Docker crashed repeatedly mid image-pull in this environment, so verified against the linked remote Supabase project instead (.env temporarily pointed at it via `supabase projects api-keys`, restored after) with a temp test user (created+deleted via admin API) driven by Playwright at 1440px/375px+touch and under reducedMotion:'reduce' — confirmed 19 pills with one selected, selected pill's bg matches the sky-600 token, Today button disable/enable behavior, pill/arrow clicks move the selection, both grids pick up the correct slide-direction animation class, zero horizontal scroll at 375px, and no transform under reduced motion.
US-030: added lib/schedule/now-line.ts's useNowOffsetPx(date) hook (returns the current-time pixel offset via lib/schedule/hours.ts's offsetForTime, null unless `date` is today, refreshes every 60s) and components/schedule/now-line.tsx's <NowLine /> (sky-300 border-t-2 line + optional leading dot); wired into the US-027 mobile calendar's grid area and the US-028 desktop grid's time gutter (with dot) plus each room column (without dot). tsc/lint/build clean. Started the local Supabase stack (had to remove a stale leftover container from a prior crashed run) and verified with Playwright + a temp test user/seeded today booking at 1280px/375px: now-line present only on today, disappears after navigating to a prior day, and its rendered top offset (401.33px) matches the exact expected math for the current time-of-day.
US-031: added components/schedule/event-block.tsx (EventBlock, a real <button> positioned by caller top/height, font-mono time range, service-category left-accent+tint via new lib/schedule/service-colors.ts, optional requester line, status dot, collapses to time-only under 40px, pending=dashed/70% opacity, cancelled=struck-through+greyed, aria-label "{room}, {time}, {status}"; hover opens a Base UI Popover anchored via ref, click opens a US-008 kit Modal detail sheet) and lib/schedule/event-layout.ts's layoutOverlappingEvents() (interval-graph-coloring column packer + 5 unit tests). Wired both into mobile-day-calendar.tsx and desktop-resource-grid.tsx, replacing their inline event <div>s; deliberately left `service`/`requesterName` unpassed from those two call sites since bookings_schedule (US-015) intentionally excludes identity/service per CLAUDE.md's RLS scheduling rule — the component supports both props, just isn't fed them by the shared schedule view. Verified: tsc/lint/build clean, npm test 31/31 (5 new); started local Supabase, seeded 3 temp bookings (overlapping approved+pending pair, one 15-min booking) for the seed admin user, drove Playwright at 1440px/375px confirming column-split widths, dashed/opacity pending styling, time-only collapse with no overflow, hover popover and click-modal both showing correct room/time/status, correct aria-labels, zero horizontal scroll, and no new console errors; deleted the seeded bookings after.
US-033: added supabase/migrations/20260722190000_create_favorite_rooms_schema.sql — favorite_rooms(user_id FK, room_id FK, created_at, unique(user_id,room_id)), RLS with select/insert/delete-own policies only (no update), matching grants. Verified: npx supabase db reset applies cleanly; psql confirms table shape, unique constraint, FKs, and the 3 policies scoped to auth.uid(); npx tsc --noEmit passes.
US-032: added supabase/migrations/20260722180000_add_room_building_floor_type.sql (nullable building/floor/room_type columns, since the rooms table had none of these facets at all — additive, backfills building/floor from existing location text, seed.sql updated with explicit values for the 4 seed rooms) plus lib/rooms-filters.ts (ScheduleRoom type, CAPACITY_BUCKETS, computeRoomFacets deriving options from real data, roomMatchesFilters, groupRoomsByBuilding/buildingGroupSpans) and components/schedule/room-filter-bar.tsx (multi-select FilterChip-styled dropdowns per facet with a count badge, built on US-010's DropdownMenu CheckboxItem). Wired into schedule-content.tsx (owns filter state, narrows rooms fed to both MobileDayCalendar's selector and DesktopResourceGrid's columns) and added an optional sticky group-header row to desktop-resource-grid.tsx spanning each building's contiguous columns. tsc/lint/build clean; verified via local Supabase db reset + Playwright with a temp test user (created/deleted via admin API) at 1440px/375px — Building filter narrows both mobile selector and desktop columns with a count badge, Clear filters restores all rooms, sticky group headers span the correct column counts, Room type filter narrows the mobile selector, zero horizontal scroll at 375px, no new console errors.
US-034: added app/(app)/schedule/actions.ts toggleFavoriteRoom server action (RLS-scoped insert/delete, mirrors notifications/actions.ts pattern) + lib/rooms-filters.ts sortRoomsByFavorite(); wired into schedule-content.tsx (optimistic favorite Set state, toast on failure) and pin toggle UI in mobile-day-calendar.tsx (IconButton next to the room Select) and desktop-resource-grid.tsx (IconButton per column header), with pinned rooms floating first in both the mobile dropdown and desktop column order. typecheck/lint/build/tests pass; verified against local Supabase (db reset + a temp Playwright-driven test user) that pin/unpin reorders and persists across reload, and a direct RLS check confirmed a spoofed user_id insert is rejected.
US-035: added a toolbar to components/schedule/desktop-resource-grid.tsx with a searchable room quick-jump (Base UI Combobox filtering rooms by name; on select, scrollToRoom() scrolls the grid's scrollRef container to the target room column via per-room refs) and a "Hide free rooms" toggle Button (aria-pressed) that filters rendered columns to only rooms with a bookings_schedule row today, deriving busyRoomIds from the already-fetched bookings state; all render call sites now iterate the filtered visibleRooms. tsc/lint/build clean. Verified with Playwright against local Supabase using a temp test user, 10 extra temp rooms, and a seeded today-booking (all cleaned up after) at 1280px: hide-toggle narrows 14 columns to the 1 busy room and restores on un-toggle, quick-jump filters to a typed room name and clicking it scrolls the grid (scrollLeft 0 -> 661 of 1274px), no new console errors.
US-036: added components/schedule/drag-create-overlay.tsx (shared ghost block for pending drag range / post-drop conflict warning) and lib/schedule/hours.ts's timeForOffset() (pixel->snapped time, inverse of offsetForTime). Desktop: mousedown on an empty room-column cell (bailing out over an EventBlock button) starts a drag tracked via window mousemove/mouseup; on release, the pending range runs through the existing findConflictingBookings against that room's bookings — conflict shows a 3s inline red warning and no navigation, otherwise router.push's to /bookings/new?room=&date=&start=&end= (read by the existing US-017 booking sheet). Mobile: rewrote the touch handlers as native non-passive listeners (React's synthetic touchmove can't preventDefault) that disambiguate vertical drag-to-create vs the existing horizontal day-swipe once movement passes a 10px threshold, reusing the same conflict-check/navigate logic. Verified: tsc/lint/build clean (13 routes unchanged); drove local Supabase (db reset) + Playwright at 1440px (mouse) and 390x844/hasTouch (CDP-dispatched native touch events) with a seeded 10-11am Choir Room booking (deleted after) — confirmed conflict warning + no navigation when dragging into the booking, correct /bookings/new?...&start=&end= navigation when dragging an empty slot (both mouse and touch), and no regression to the horizontal day-swipe.
US-037: rewrote app/(app)/rooms/page.tsx to render the US-013 RoomCard in a responsive grid (grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4) instead of the old shadcn Card list; availability dot computed server-side from today's approved bookings_schedule rows (busy if now falls within a room's approved slot, else free); empty case now uses the US-010 EmptyState. Confirmed via git log that the prior /rooms page never had any filtering/search to preserve. tsc/lint/build clean (13 routes unchanged); verified with local Supabase db reset + Playwright using a temp test user and a seeded "busy right now" booking (cleaned up after) at 1280px/375px — 4 room cards in a 4-col desktop grid, correct Busy now/Free labels, card click navigates to /rooms/[id], zero horizontal scroll at 375px (1-col grid).
US-038: rewrote app/(app)/rooms/[id]/page.tsx as a rich room detail card: color header banner, capacity/location, an amenity grid (icon+label, reusing a newly-exported amenityIcon helper from components/kit/room-card.tsx instead of duplicating the icon map), a usage-rules list (rules split on newlines), and a new mini 7-day availability strip (queries bookings_schedule for this room's approved bookings over the next 7 days, per-day free/busy dot using the existing status-token colors). "Book this room" CTA links to /bookings/new?room={id}, which the existing US-017 BookingSheet already pre-fills via searchParams. tsc/lint/build clean (13 routes unchanged); verified with local Supabase (db reset) + a Playwright script using a temp test user and a seeded today 10-11am approved Choir Room booking (cleaned up after, stack stopped afterward) at 1280px/375px — header/capacity/location/amenities/rules/CTA all render, strip shows Busy today and Free the other 6 days, zero horizontal scroll, and the CTA opens the booking sheet pre-selected to this room.
US-039: added components/shell/global-availability-search.tsx (date/time/capacity/amenity filter reusing lib/bookings/conflict-check's findConflictingBookings, no duplicate overlap logic; results render as US-013 RoomCard grid linking to /schedule?room=&date=); wired into the US-023 top bar's search Modal, replacing the placeholder, with DesktopTopBar/MobileShell now fetching rooms server-side; schedule page reads room/date search params to seed the mobile calendar's initial room and the desktop grid's initial scroll-to-room; RoomCard gained an onClick passthrough so selecting a result also closes the search panel. tsc/lint/build clean; verified with local Supabase (supabase start) + Playwright using a temp test user with a seeded approved 9-10am booking and a seeded pending 11-12pm booking on two rooms (cleaned up after, stack stopped afterward) at 1440px/375px — approved-conflict room correctly excluded, pending-conflict room shows "Busy now", clicking a result closes the panel and lands on /schedule pre-filtered to the right date with the grid scrolled to that room's column, and no horizontal scroll on the mobile bottom-sheet variant.
US-040: moved the existing pending-approval-queue page verbatim to app/(app)/admin/requests/page.tsx (renamed AdminRequestsPage; its "Home" button now links to /admin) and updated approveBooking/rejectBooking's redirects in app/(app)/admin/actions.ts from /admin to /admin/requests (no business-logic change, same conflict re-check on approve). Rewrote app/(app)/admin/page.tsx as the overview dashboard: a 3-card KPI row (today's bookings, pending count, utilization % = approved-hours-today / (room count x schedule-hour span), font-mono tabular-nums) plus a Recent activity list (last 10 bookings by updated_at, room/requester/StatusBadge/relative-time/View link) with a US-010 EmptyState when empty. Requester names needed a separate profiles-by-user_id query since bookings has no direct FK to profiles (a nested embed throws PGRST200) -- matched the existing pattern from the requests page. tsc/lint/build clean (14 routes, +1 for /admin/requests). Verified with local Supabase (db reset) + Playwright using the seed admin user and two temp today bookings (approved + pending, deleted after) at 1280px/375px -- KPI cards read 2/1/2%, recent-activity list renders both rows correctly, View link's target page renders, /admin/requests still lists+processes the pending booking, zero horizontal scroll at 375px. Stack and dev server stopped afterward.
US-041: refactored app/(app)/admin/actions.ts's approve/reject into shared redirect-free helpers (approveBookingById/rejectBookingById, same conflict re-check + race-guard update, unchanged) reused by four client-callable server actions returning structured {id, ok, error?} results: approveBookingAction/rejectBookingAction (single) and bulkApproveBookingsAction/bulkRejectBookingsAction (array of ids); reject now requires a non-empty reason (UI + defensive server check). Added components/admin/admin-requests-table.tsx rendering pending requests via the US-011 kit Table: Room column deep-links to /schedule?room=&date= (US-027/028), sortable Requester/Date&time/Service columns, inline Approve/Reject buttons per row, a checkbox column + "select all" toolbar with bulk Approve/Reject selected, and a US-008 Modal with a required reason Textarea shared by single/bulk reject; failures (including per-row conflict-check failures during bulk approve) show as a toast plus an inline red banner listing which rows failed and why. Rewrote app/(app)/admin/requests/page.tsx to feed it from the same unchanged pending-bookings/profiles query, dropping the old ad hoc Card list and ?error= redirect mechanism. tsc/lint/build clean (14 routes unchanged). Verified with local Supabase (db reset) + Playwright using the seed admin user and 4 temp bookings for a temp member (including one pair seeded to force a real approve-time conflict; all cleaned up, stack stopped after) at 1280px/375px -- room deep-link href correct, reject submit disabled until a reason is typed, single reject/approve both work, "Select all" + bulk approve correctly re-ran the conflict check per row, kept the genuinely-conflicting row pending, and reported it via both toast and inline banner, zero horizontal scroll on mobile.
US-042: added supabase/migrations/20260722200000_add_room_code_category_color.sql — nullable unique code text and nullable category_color text (check-constrained to the 8 calendar palette keys: coral/amber/teal/violet/rose/lime/sky/slate) on rooms, plus an UPDATE backfilling code/category_color for any rows that already existed before the migration. Verified with npx supabase db reset (migration applies cleanly on top of existing schema; confirmed unique/check constraints and backfill logic via direct psql query) and npx tsc --noEmit (clean).
US-043: added supabase/migrations/20260722210000_add_room_admin_write_rls.sql granting authenticated insert/update/delete on rooms and adding rooms_insert_admin/rooms_update_admin/rooms_delete_admin RLS policies (mirroring bookings_update_admin's public.is_admin() pattern) so RLS is the real enforcement boundary. Added app/(app)/admin/rooms/actions.ts (createRoomAction/updateRoomAction/deleteRoomAction behind the same requireAdmin() guard as admin/actions.ts, plain RLS-scoped client); deleteRoomAction blocks deletion when the room has a pending booking or an approved booking that hasn't ended yet, reusing lib/dates.ts's isBookingPast. Added lib/rooms/category-colors.ts (US-042's 8-key palette + label/swatch maps), components/admin/category-color-picker.tsx (picker + table swatch), components/admin/room-form-sheet.tsx (kit Modal form sheet, mirrors booking-sheet.tsx; amenities editor via removable FilterChips + suggestions + free-text add), components/admin/rooms-table.tsx (US-011 kit Table with Add-room button, kebab Edit/Delete, delete-confirm Modal), and app/(app)/admin/rooms/page.tsx (server component; the sidebar's already-wired "Manage Rooms" link now resolves). tsc/lint/build clean (15 routes, +1). Verified with local Supabase (db reset applied the new migration + confirmed the 3 RLS policies via psql) and Playwright against the dev server using the seed admin user: created/edited/deleted a room end-to-end (all fields persisted), then seeded a temp room with a future approved booking via SQL and confirmed Delete was blocked with the exact inline error and the room stayed in the table (cleaned up after); zero horizontal scroll at 375px with the Add-room sheet rendering as a proper bottom sheet.
