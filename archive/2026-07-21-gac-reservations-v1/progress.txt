# Ralph Progress Log
Started: Sun Jul 19 12:02:39 EEST 2026
---
US-001: Scaffolded Next.js 15.5.20 App Router + TypeScript + Tailwind v4 + shadcn/ui (Button installed) at repo root; placeholder home page builds. Verification: npx tsc --noEmit clean, npm run lint clean, npm run build succeeds, dev server manually curl-checked (200, renders Button).
US-002: Wired up Supabase clients (browser, server, admin) and .env.local.example; installed @supabase/supabase-js, @supabase/ssr, server-only. Verification: npx tsc --noEmit clean, npm run lint clean.
US-003: Added supabase/migrations/20260719000000_create_core_schema.sql with booking_status enum and rooms/profiles/bookings tables (FKs, status CHECK via enum, end_time>start_time check, indexes). Verification: npx tsc --noEmit clean (SQL-only change).
US-004: added RLS policies migration for rooms/bookings/profiles (is_admin() helper, own-row scoping, admin-only approve/reject, role self-edit block via trigger); npx tsc --noEmit passes
US-005: added profile-on-signup trigger migration and supabase/seed.sql (4 sample rooms, 1 promoted admin); verified via `npx supabase db reset` against local Docker Postgres that rooms are seeded, admin profile is promoted, and a fresh auth.users insert gets a default role='member' profile; npx tsc --noEmit passes
US-006: added login/signup pages + server actions and a logout server action using the cookie-based Supabase server client; home page shows Log in/Sign up or Signed-in-as + Log out depending on session. Verified end-to-end with a Playwright-driven browser against local Supabase: signup, session persists across refresh, logout, invalid-credential error message, valid login. npx tsc --noEmit and npm run lint pass.
US-007: added middleware.ts (redirects unauthenticated users to /login for all routes, bounces authenticated users off /login+/signup) and app/admin/layout.tsx (server-side role check against profiles, redirects non-admins to /) with a placeholder app/admin/page.tsx. Also fixed a blocking bug discovered during verification: US-004's RLS migration never granted base table privileges to the authenticated role, so RLS-permitted queries still failed with "permission denied" — added supabase/migrations/20260719000300_grant_table_privileges.sql. Verified end-to-end with Playwright against a fresh `supabase db reset`: unauth -> /login, member bounced from /admin, seeded admin reaches /admin. npx tsc --noEmit and npm run lint pass.
US-008: added lib/bookings/conflict-check.ts (findConflictingBookings/hasConflict pure overlap logic keyed on room_id+date+start/end time, filtered to pending/approved, with excludeBookingId support for edit re-checks) plus fetchConflictingBookings, a Supabase-backed wrapper future stories will import for availability/create/approve/edit. Added vitest (first story needing "Tests pass" per CLAUDE.md) with vitest.config.ts and lib/bookings/conflict-check.test.ts. Verification: npx tsc --noEmit clean, npm run lint clean, npm run test 10/10 passing.
US-009: added supabase/migrations/20260719000400_add_bookings_schedule_view.sql (public.bookings_schedule, a security-invoker=false view exposing only id/room_id/date/start_time/end_time/status for pending+approved bookings, granted to authenticated) since the existing bookings RLS policy only lets a member read their own rows but the schedule must show everyone's slots without leaking user_id/notes/service. Added app/schedule/page.tsx + components/schedule/schedule-view.tsx (room filter, From/To date range, Calendar/List tabs, amber/green status badges) plus shadcn badge/select/tabs and lib/dates.ts. Verified end-to-end with Playwright against local Supabase: calendar view groups seeded bookings by day, list view is flat, room filter narrows correctly (fixed a bug where the Select showed the raw room id instead of its name), no member identity/notes visible, 375px viewport has zero horizontal overflow. npx tsc --noEmit, npm run lint, npm run test (10/10) all pass.
US-010: added app/rooms (list) + app/rooms/[id] (detail) pages and lib/rooms.ts ("Not specified" fallback for empty capacity/amenities/location/rules); linked room names in schedule-view.tsx and added a Rooms nav button on the schedule page. Verified end-to-end with Playwright against local Supabase (schedule row and room-list card both reach the detail page; null fields render "Not specified"). npx tsc --noEmit, npm run lint, npm run test (10/10) all pass.
US-011: added app/availability/page.tsx + components/availability/availability-search.tsx — a date/start-time/end-time/optional-capacity search form that queries the bookings_schedule view for the chosen date and reuses the shared findConflictingBookings util per room: approved-conflict rooms are excluded, pending-conflict rooms show a "Requested — pending approval" badge, others show "Available"; capacity filter drops rooms with insufficient/null capacity; empty state shown when nothing qualifies. Linked from home page and schedule page nav. Verified end-to-end with Playwright against local Supabase (db reset + one approved and one pending seeded booking): correct exclusion/pending/available results, capacity filter, empty state, a non-conflicting slot re-including the previously excluded room, and zero horizontal overflow at 375px. npx tsc --noEmit, npm run lint, npm run test (10/10) all pass.
US-012: added app/bookings/new (request form: room/date/start/end/service-dropdown/notes) + app/bookings/actions.ts requestBooking server action + app/bookings (minimal "My bookings" flat list as the redirect/confirm target, to be expanded by US-016) + lib/bookings/services.ts (fixed BOOKING_SERVICES dropdown constant per PRD FR-9). Client-side conflict check warns (non-blocking) on field change via bookings_schedule + findConflictingBookings; server action re-checks conflicts via fetchConflictingBookings and blocks insert with no row written, and separately enforces the 5-open-pending-bookings cap. Found and fixed two real bugs while verifying: fetchConflictingBookings was querying the RLS-restricted `bookings` table (missed other members' conflicts) — switched to the `bookings_schedule` view; and neither the form nor the action normalized "HH:MM" input times to Postgres's "HH:MM:SS" before comparing/storing, causing false-positive conflicts on adjacent non-overlapping slots — added lib/dates.ts#normalizeTimeString. Verified end-to-end with Playwright against a fresh `supabase db reset`: valid insert + redirect to /bookings; duplicate-slot request warned client-side then blocked server-side with zero rows written; approved-booking conflict also blocked; 5-pending cap enforced (6th request rejected, no 6th row); adjacent non-overlapping slots correctly allowed; 375px viewport has zero horizontal overflow. npx tsc --noEmit, npm run lint, npm run test (10/10) all pass.
US-013: added app/admin/page.tsx + app/admin/actions.ts (approve/reject server actions with server-side admin role re-check) and supabase/migrations 20260719000500 (admin select policies on bookings/profiles) + 20260719000600 (service_role table grants, same class of gap as US-007). Fixed a real bug: approveBooking's conflict re-check reused the default pending+approved status filter, so two overlapping pending requests always blocked the FIRST approval too; added an optional statuses param to fetchConflictingBookings and pass ['approved'] for the approval race guard. Verified end-to-end with Playwright against a fresh `supabase db reset`: non-admin blocked from /admin, admin approves one of two conflicting pending requests, the sibling's approval is blocked with a conflict error, reject-with-reason works, each member sees the correct final status. npx tsc --noEmit, npm run lint, npm run test (10/10) all pass.
US-014: added components/bookings/cancel-booking-button.tsx (shadcn alert-dialog confirmation, `npx shadcn add alert-dialog`) and app/bookings/actions.ts#cancelBooking, wired into app/bookings/page.tsx for bookings that are pending/approved and not past (new lib/dates.ts#isBookingPast helper). Server action re-verifies ownership/status/not-past before setting status='cancelled'; the existing bookings_update_own RLS policy independently blocks cross-user cancellation. No new migration needed — bookings_schedule and the conflict-check utility already filter to pending/approved, so a cancelled slot is immediately free elsewhere. Verified end-to-end with a Playwright + supabase-js script against a fresh `supabase db reset`: future pending/approved bookings show a working Cancel button (with confirm dialog) that sets status to cancelled; a past approved booking shows no Cancel button and is untouched; bookings_schedule excludes cancelled ids afterward; a second user's signed-in client cannot update the first user's booking (0 rows affected). npx tsc --noEmit, npm run lint, npm run test (10/10) all pass.
US-015: added app/bookings/[id]/edit + components/bookings/edit-booking-form.tsx + lib/bookings/status.ts (isBookingModifiable, reused by /bookings list and the edit page); updateBooking server action re-checks ownership, modifiability, and conflicts (excluding own id), reverts status to pending on room/date/time change. Verified end-to-end via Playwright against a fresh `supabase db reset`: conflict-blocked reschedule, successful reschedule reverting approved->pending, past-booking edit blocked, cross-user edit blocked, no horizontal overflow at 375px. npx tsc --noEmit, npm run lint, npm run test (10/10) all pass.
US-016: rewrote app/bookings/page.tsx into a shadcn Tabs view (Upcoming/Pending/Past/Cancelled with live counts), rows now link to a new app/bookings/[id]/page.tsx detail view (which holds the Edit/Cancel actions) instead of exposing them inline. Added lib/bookings/status.ts#bucketForBooking: cancelled/rejected are always terminal, everything else falls to "past" once its end time has elapsed regardless of status. Verified end-to-end with a Playwright + supabase-js script against a fresh `supabase db reset`: one user seeded with 4 bookings (approved future, pending future, approved past, cancelled future) plus a second user's booking — tab counts read 1/1/1/1 correctly bucketed, the second user's booking never appears (RLS-enforced), the upcoming booking's detail page shows Edit+Cancel while the past booking's detail page shows neither, 375px viewport has zero horizontal overflow. npx tsc --noEmit, npm run lint, npm run test (10/10) all pass.
