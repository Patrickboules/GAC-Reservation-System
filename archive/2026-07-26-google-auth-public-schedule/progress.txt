# Ralph Progress Log
Started: Sun Jul 26 14:31:03 EEST 2026
---

US-001: enabled Supabase Google OAuth provider in config.toml (env-backed client_id/secret, skip_nonce_check=true, no domain restriction), added local /auth/callback + prod placeholder redirect URLs, documented Google env vars in .env.example, and added Google Cloud + Supabase dashboard OAuth setup to DEPLOYMENT.md; typecheck passes.
US-002: added app/auth/callback/route.ts (exchangeCodeForSession, validated relative next -> /schedule, error -> /login?error=); typecheck + lint pass
US-003: added 20260726120000_grant_anon_schedule_read.sql granting anon SELECT on bookings_schedule and rooms (+ rooms_select_anon RLS policy); no anon grant on bookings/profiles/favorite_rooms/notifications. Verified by replaying all migrations against a scratch Postgres 18 (Docker unavailable for supabase db reset) — clean apply; anon reads schedule/rooms and is denied on protected tables. Typecheck passes.
US-004: replaced email/password login with a single Continue-with-Google button + signInWithOAuth server action (redirectTo /auth/callback carrying next); deleted app/signup (page+action) so /signup 404s. Typecheck passes; verified in browser (Google button renders, no email/password, /signup 404, Supabase authorize 302 -> Google consent).

US-005: public-schedule middleware — PUBLIC_PREFIXES allowlist (/schedule, /login, /auth/callback) + exact "/"; signed-out visitors to gated routes redirect to /login?next=<path>; signed-in users only bounced off /login to /schedule; dropped /signup. Typecheck + lint pass; curl-verified /schedule=200, booking/admin/notification/availability routes 307 to /login?next=.
US-006: schedule is landing page + renders for signed-out users. TopBar/sidebar show a Sign in link (to /login) in place of the account menu/logout when user===null; threaded authenticated=Boolean(user) through Desktop/Mobile top bars and Sidebar. Verified against dev server: / 307->/schedule, /schedule 200 with Sign in affordance and no Sign out/account-menu, /bookings/new 307->/login. Typecheck + lint pass.

US-007: booking creation prompts sign-in and preserves the selected slot. Threaded authenticated=Boolean(user) from schedule/page.tsx -> ScheduleContent -> TimelineGrid; signed-out drag-to-create now routes to /login?next=<encoded /bookings/new?room=&date=&start=&end=> instead of straight to the form (signed-in flow unchanged). Booking form already prefills from room/date/start/end params; login->callback->prefill round-trip already wired. Typecheck + lint pass; Playwright-verified on a temp harness route (deleted) that a signed-out drag redirects to /login with the slot preserved in next. Actual Google-consent completion + anon room visibility on hosted DB (US-003 not applied there) not drivable locally.
US-008: migration 20260726130000 updates handle_new_user() to populate profile display_name from Google OAuth metadata (full_name/name) with email fallback, role stays 'member'; no self-serve admin path (role check constraint + prevent_role_self_update trigger); admin routes still gated on role='admin'; DEPLOYMENT.md documents the grant-admin SQL. Verified: migrations replay cleanly on scratch PG18 (trigger names populated correctly), typecheck passes, anon /admin/* 307s to /login.
US-009: updated CLAUDE.md + tasks/prd-gac-reservations-v1.md for public schedule + Google-only auth (superseding note, corrected email/password refs); typecheck passes
