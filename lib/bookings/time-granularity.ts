/** Time-of-day snapping interval used by the booking time-range picker (minutes). */
export const BOOKING_TIME_STEP_MINUTES = 30;

/** Latest allowed end time for any booking — requests/edits ending later than this are rejected. */
export const LATEST_BOOKING_END_TIME = "22:30:00";
